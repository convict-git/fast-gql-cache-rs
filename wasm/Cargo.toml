[package]
name = "fast-gql-cache-rs-wasm"
version = "0.1.0"
edition = "2021"
description = "Rust WASM core for fast-gql-cache-rs"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
